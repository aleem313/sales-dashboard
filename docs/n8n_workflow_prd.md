# PRD — n8n Workflow: "multiple webhooks"

| | |
|---|---|
| **Document version** | 1.2 |
| **Last reviewed** | 2026-05-12 |
| **Workflow ID** | `EWnZg3svZWwcIRs4` |
| **Workflow name** | `multiple webhooks` |
| **n8n instance** | `ikonicdev.app.n8n.cloud` (n8n cloud, v2.42.3) |
| **Status** | Active in production |
| **Owners** | Rising Lions Analytics — Admin (M. Waqas) |
| **Related docs** | `CLAUDE.md`, `docs/multiple webhooks (working flow).json`, `docs/taskboard_prd.md`, `docs/n8n_relevancy_classifier_core_prd.md` (sub-workflow, pending splice in Phase 7) |

---

## 1. Executive summary

The "multiple webhooks" workflow is the **production ingestion pipeline** for Upwork lead automation. It receives jobs scraped by Vollna (one webhook per agent profile), uses an LLM to draft an Upwork proposal, and writes the result into both the Task Board and the analytics Dashboard on the **Contabo self-hosted deployment** (`http://157.173.110.62`).

A single Vollna POST to a per-agent webhook ends in:
1. A new task on the Task Board (Contabo, via `POST /api/v1/webhooks/tasks`),
2. A new job event on the Dashboard (Contabo, via `POST /api/webhook/n8n`).

The proposal generated by Claude is the work product agents copy and submit on Upwork.

> **Historical note:** Until 2026-04-29, the workflow dual-wrote to both Vercel (`sales-dashboard-snowy-beta.vercel.app`) and Contabo. The Vercel sinks were removed when the user consolidated infrastructure on Contabo. See §12.

---

## 2. Goals & non-goals

### 2.1 Goals
- **Per-agent isolation.** Each of 8 agent profiles has its own dedicated webhook URL; Vollna can fail or pause one agent without affecting others.
- **Single source of truth for proposals.** Claude generates one proposal per qualifying job; we never produce duplicates.
- **Resilient fan-out.** A failure of any downstream sink (Board API, Dashboard webhook) must NOT block the others.
- **Operational observability.** Every event lands in n8n's execution log with the agent, profile, outcome, and reason fields populated.

### 2.2 Non-goals
- **Status tracking.** Job status (Won, Lost, In Chat, etc.) is owned by the Task Board (column moves) — not by this workflow.
- **De-duplication of jobs across the same workflow run.** Vollna is responsible for not re-posting the same job. The workflow has lightweight per-`job.id` static-data tracking but does not enforce uniqueness.
- **Active-hours filtering.** No business-hours or weekend gate is enforced at the workflow level. (A `Check Active Hours` node previously existed but was removed on 2026-04-29 — see §12.)
- **Automatic retries from this workflow.** The 4 active sinks rely on `neverError: true` rather than retry. See §10.4.

---

## 3. Stakeholders & users

| Role | Identity | Interaction |
|---|---|---|
| **Lead source** | Vollna (Upwork scraper) | Pushes one webhook per matched job per agent |
| **Agents** (8) | Sana, Laiba, Khansa, Saim, Shayan, Craig, Rebekah, Nawal | Each owns one or more profiles. Their dedicated webhook URLs are configured in Vollna |
| **Profile-mapping API** | `/api/profiles/mapping` (Contabo primary, Vercel fallback) | Admin-managed mapping of profile name → agent. Workflow fetches it on every execution. Vercel fallback retained pending separate decision — see §13. |
| **AI provider** | Anthropic Claude Haiku 4.5 (`claude-haiku-4-5-20251001`) | Drafts proposals; credential `Aleem Anthropic account` (id `fVtEWZhGXzEBZDoS`) |
| **Sinks** | Contabo Board API, Contabo Dashboard webhook | All receive the same processed event, with adapter-specific payload shapes |
| **Admin** | M. Waqas + delegates | Owns workflow edits via n8n MCP and direct UI access |

---

## 4. System architecture

### 4.1 High-level topology

```
                ┌──────────────────────────────────────────┐
8x Vollna ─POST─►│ Webhook - {Agent}  (n8n cloud)          │
                ├──────────────────────────────────────────┤
                │ Respond - {Agent}  (immediate 200 OK)    │  ← Vollna closes the connection here
                ├──────────────────────────────────────────┤
                │ Merge All Webhooks   (v3.2, 8 inputs)    │
                ├──────────────────────────────────────────┤
                │ Process Job          (parse, route, dedup)│
                ├──────────────────────────────────────────┤
                │ Route Job  (switch on _result)           │
                │   ├─proceed────► Build GPT Input         │
                │   └─other──────► Format Dashboard Event  │
                ├──────────────────────────────────────────┤
                │ AI pipeline:                             │
                │   Build GPT Input                        │
                │   → AI Agent (Claude Haiku 4.5)          │
                │     ├─ai_languageModel: Anthropic        │
                │     └─ai_outputParser: Structured        │
                │   → Merge Proposal w/ Job Data           │
                │   → Proposal OK?                         │
                │     ├─true ─► Format ClickUp Task        │
                │     └─false ► Extract Error              │
                ├──────────────────────────────────────────┤
                │ Format ClickUp Task fans out 2-wide:     │
                │   ├─► Create Board Task - Self-Hosted → Contabo POST /api/v1/webhooks/tasks │
                │   └─► Format Dashboard Event             │
                │       └─► Send to Self-Hosted Dashboard  → Contabo POST /api/webhook/n8n │
                └──────────────────────────────────────────┘
```

### 4.2 Node inventory (34 nodes)

| Section | Nodes |
|---|---|
| **Webhooks** (8) | Webhook - Sana, Laiba, Khansa, Saim, Shayan, Craig, Rebekah, Nawal |
| **Respond-to-webhook** (8) | Respond - Sana, Laiba, Khansa, Saim, Shayan, Craig, Rebekah, Nawal |
| **Aggregation** | Merge All Webhooks (v3.2, `numberInputs: 8`) |
| **Logic / processing** | Process Job, Route Job (Switch v3), Build GPT Input, Merge Proposal with Job Data (Set v3.4), Proposal OK? (IF v2), Format ClickUp Task, Extract Error, Format Dashboard Event |
| **Relevancy classifier (Phase 7, 2026-05-12)** | IF - Classifier Enabled (kill-switch on `$env.RELEVANCY_CLASSIFIER_ENABLED`), Score Relevancy (executeWorkflow → `hi71jhPU8tmq7hEp`), Route Verdict (Switch v3.2 on `effective_decision + classifier_mode`), End (Audit Only) (noOp terminal) |
| **AI pipeline** | AI Agent - Proposal Writer, Claude Chat Model - Proposal, Structured Output Parser |
| **Sinks** (2 HTTP + 1 fan-out) | Create Board Task - Self-Hosted, Send to Self-Hosted Dashboard |

### 4.3 Workflow settings

| Setting | Value | Rationale |
|---|---|---|
| `active` | `true` | Production |
| `executionOrder` | `v1` | n8n's modern parallel execution model |
| `binaryMode` | `separate` | Keep binary data out of the JSON event log |
| `timezone` | `Asia/Karachi` | All `new Date()` and time-based logic operates in PKT by default |
| `callerPolicy` | `workflowsFromSameOwner` | Only same-owner workflows can call this one as a sub-workflow |
| `availableInMCP` | `false` | This workflow is not exposed as a tool to MCP-driven agents |
| `saveExecutionProgress` | `true` | Step-by-step execution data retained for debugging |

---

## 5. Functional requirements (per node)

### 5.1 Webhook nodes (8)

**Type:** `n8n-nodes-base.webhook` v2.1
**Method:** `POST`
**Response mode:** `responseNode` (a separate Respond node sends the 200 OK)
**onError:** `continueRegularOutput`

| Agent | Webhook path (URL suffix) | Webhook node ID | Style |
|---|---|---|---|
| Sana | `6f516263-704b-4b10-86e5-012e06f33ef9` | `cc0132a7-c18f-473b-a61e-41973ae849f4` | UUID (legacy) |
| Laiba | `590b2765-1695-46d6-8403-5101107c199b` | `035e375f-fd9d-4bdb-8b3b-fb942f9edc68` | UUID (legacy) |
| Khansa | `f9985fe6-b639-40a0-9c3b-a19b4bdab490` | `84b0f382-1202-4a3a-913a-b81d061da8c2` | UUID (legacy) |
| Saim | `b0ae892c-b7de-4c29-ad2f-2af0b3acbe78` | `b289501d-2ce2-4172-a5c0-b4dc5a60c721` | UUID (legacy) |
| Shayan | `fd520319-25f9-474e-a72f-f05ef13d6dc8` | `bb63bef8-d0c5-41d3-8db3-07d41311aeb8` | UUID (legacy) |
| Craig | `02d7de17-61db-4980-9444-1ad527079e37` | `2a2dc271-5fdd-4f7c-bcd0-b5d4fc02c9aa` | UUID (legacy) |
| Rebekah | `rebekah-profile-webhook` | `3efa17c8-34d4-434a-bf42-1a0e791389a6` | Slug (current) |
| Nawal | `nawal-profile-webhook` | `0e4c251a-b689-4622-8beb-816c51251011` | Slug (current) |

**Public URL pattern:** `https://ikonicdev.app.n8n.cloud/webhook/{path}`

**Rationale for two styles:** The first six agents were provisioned via the n8n UI which auto-generates UUID paths. The two newest (Rebekah, Nawal) were provisioned via `POST /api/profiles/sync-n8n` which uses the friendly slug pattern `{name}-profile-webhook`. **Routing in `Process Job` is path-aware for the slug style only**; the UUID-style webhooks fall back to `body.filter_name` lookup. This MUST work for the legacy 6 because Vollna does send `filter_name` in the body.

### 5.2 Respond-to-webhook nodes (8)

**Type:** `n8n-nodes-base.respondToWebhook` v1.1
**Purpose:** Close the HTTP connection back to Vollna immediately so Vollna does not wait for the AI pipeline to finish (which can take 5–60 seconds).
**Output:** Each Respond-X feeds the matching `Merge All Webhooks` input index — see §5.3.

### 5.3 Merge All Webhooks

**Type:** `n8n-nodes-base.merge` v3.2
**Parameters:** `numberInputs: 8`
**Input wiring (CRITICAL — restored 2026-04-29):**

| Input index | Source |
|---|---|
| 0 | Respond - Sana |
| 1 | Respond - Laiba |
| 2 | Respond - Khansa |
| 3 | Respond - Saim |
| 4 | Respond - Shayan |
| 5 | Respond - Craig |
| 6 | Respond - Rebekah |
| 7 | Respond - Nawal |

**Why v3.2 specifically:** v3.2 emits downstream when ANY input has data and gracefully ignores empty inputs. v3 emits downstream on every input fire — with 8 inputs, that triggers the AI pipeline 8x for one event, which historically caused OOM crashes on the n8n cloud instance. **Do not downgrade to v3.**

**Why unique input indices matter:** Multiple agents firing concurrently must hit different inputs so n8n's per-execution context isolates them. Sharing an input causes per-agent telemetry blending and (under burst load) item ordering anomalies in downstream nodes.

**Output:** Single edge → `Process Job`.

### 5.4 Process Job

**Type:** `n8n-nodes-base.code` v2 (JS sandbox)
**Purpose:** Parse the inbound webhook, identify the agent profile, normalize the job payload, and emit a `_result` token for `Route Job`.

**Logic:**
1. **Fetch profile mapping** from `/api/profiles/mapping`. Tries Contabo first (`http://157.173.110.62/api/profiles/mapping`), falls back to Vercel (`https://sales-dashboard-snowy-beta.vercel.app/api/profiles/mapping`). Uses `this.helpers.httpRequest()` (n8n-sandboxed `fetch` is unavailable). Empty/error responses on both → emits `_result: 'rejected'`.
2. **Build `PATH_TO_PROFILE`** dynamically from the mapping: `{<lowercase>-profile-webhook → ProfileName}`. This is what makes routing zero-config when a new profile is added — no hard-coded list.
3. **Resolve agent profile** for the inbound event: try matching `headers.path` against `PATH_TO_PROFILE`; if no match (UUID-style webhook), fall back to `body.filter_name || body.filterName || body.filter?.name || body.filter_tag || body.filter`.
4. **Iterate `body.projects`** (Vollna can batch jobs). For each project:
   - Decode the URL (Vollna double-URL-encodes), extract `~<jobId>` from the path, fall back to `p.id || p.job_id || p.uid`.
   - Build the canonical `job` shape (id, title, description, url, budget/budgetType, skills, duration, postedDate, client country/rating/spent/hires/paymentVerified, applicants).
   - If profile name resolves: emit `_result: 'proceed'` with full job + agent metadata.
   - If profile name does NOT resolve in the mapping: emit `_result: 'no_profile'` with the failed `filterName` for triage.
5. If `body.projects` is empty / no valid jobIds: emit `_result: 'rejected'` with reason `"No valid projects in payload"`.

**Emitted `_result` values (today):** `proceed`, `no_profile`, `rejected`. The `Route Job` Switch additionally has rules for `inactive`, `duplicate`, `weekend` — those branches are unreachable until/unless `Process Job` is extended (see §11).

**Output shape (proceed case):**
```json
{
  "_result": "proceed",
  "job": { "id": "...", "title": "...", "description": "...", "url": "...", "budget": "...", "budgetType": "...", "skills": [...], "duration": "...", "postedDate": "...", "clientCountry": "...", "clientRating": "...", "clientSpent": "...", "clientHires": "...", "paymentVerified": false, "applicants": 0 },
  "filterName": "Sana",
  "profile_id": "Sana",
  "profile_name": "Sana",
  "stack": "...",
  "assigned_agent": "...",
  "agent_clickup_id": "...",
  "clickup_list_id": "901815302064",
  "custom_gpt_instructions": ""
}
```

### 5.5 Route Job

**Type:** `n8n-nodes-base.switch` v3
**Switch field:** `$json._result`
**Rules** (in order):

| Output index | `_result` match | Connected to | Reachable today |
|---|---|---|---|
| 0 | `"proceed"` | Build GPT Input | Yes |
| 1 | `"no_profile"` | Format Dashboard Event | Yes |
| 2 | `"inactive"` | Format Dashboard Event | No (dead branch) |
| 3 | `"duplicate"` | Format Dashboard Event | No (dead branch) |
| 4 | `"rejected"` | Format Dashboard Event | Yes |
| 5 | `"weekend"` | (unconnected) | No (dead branch) |

**Fallback:** `options.fallbackOutput: "extra"` — but no edge from the fallback output. Any non-matching `_result` value is silently dropped. This is acceptable today because `Process Job` is the only emitter and it only produces matching values, but a future change to `Process Job` MUST also wire the fallback or extend the rule set.

### 5.6 Build GPT Input

**Type:** `n8n-nodes-base.code` v2
**Purpose:** Construct the user-message string for the AI Agent and pick a hook variant per profile.

**Hook rotation:**
- Per-profile static data (`$getWorkflowStaticData('global').hookRotation[profileId]`) tracks the last hook used.
- Rotation is **A ↔ B only**: `useHook = lastHook === 'A' ? 'B' : 'A'`.
- The system prompt defines three hook types (A: curiosity-led, B: prototype hook, C: psychological question). **Hook C is currently never selected** — see §11 ("Hook C dead").

**Output:** Adds `gptInput` (multi-line string with profile, stack, agent, hook selector, profile-specific instructions, job title, description (first 2000 chars), budget, skills, client context, "Write the proposal now.") to the item. Forwards everything else.

### 5.7 AI Agent - Proposal Writer

**Type:** `@n8n/n8n-nodes-langchain.agent` v3.1
**Prompt type:** `define` — `text` is taken from `{{ $json.gptInput }}`
**System message:** A ~3,500-word Upwork proposal-strategist prompt covering:
- Hard rules (word count 150–230, no emojis/exclamations/hedging, no opening with "Hi/Dear/I am", always reference something specific from the post, end with a call invitation).
- Hook rotation system: agent must print all 3 hook variants (A/B/C) at the top of every response, then write the proposal using the hook the user specified via `USE HOOK:`.
- Project Database (placeholder — not yet populated).
- Section-by-section proposal structure (screening answer, hook, relevance proof, curiosity line, problem understanding, mini-plan, differentiator, strategic question, CTA).

**Output parser:** `Structured Output Parser` returning `{ proposal: string, _proposalOk: boolean }`. `autoFix: true` so malformed outputs are repaired by the LLM.

**LLM:** `Claude Chat Model - Proposal` (Claude Haiku 4.5, temp 0.2, credential `Aleem Anthropic account`).

### 5.8 Merge Proposal with Job Data

**Type:** `n8n-nodes-base.set` v3.4
**Purpose:** The AI Agent only outputs `proposal` and `_proposalOk` — re-attach the original job/profile data via `$('Build GPT Input').item.json` so downstream nodes have the full context.

**Set fields:**
- `proposal` ← AI output
- `_proposalOk` ← AI output
- `job`, `profile_id`, `profile_name`, `stack`, `assigned_agent`, `agent_clickup_id`, `clickup_list_id`, `filterName` ← from Build GPT Input

`includeOtherFields: true` so the rest of the item is preserved.

### 5.9 Proposal OK?

**Type:** `n8n-nodes-base.if` v2
**Condition:** `$json._proposalOk === true`
- **True branch** → `Format ClickUp Task`
- **False branch** → `Extract Error`

### 5.10 Extract Error

**Type:** `n8n-nodes-base.code` v2
**Purpose:** Build a minimal `gpt_error` payload from a failed proposal item, mark the job as `gpt_failed` in static data (so retry logic — if added later — can detect it), and forward to `Format Dashboard Event`.

**Output:**
```json
{ "job_id": "...", "job_url": "...", "job_title": "...", "profile_id": "...", "profile_name": "...", "assigned_agent": "...", "error_detail": "..." }
```

### 5.11 Format ClickUp Task

**Type:** `n8n-nodes-base.code` v2
**Purpose:** Build the rich-emoji task body shared by both downstream sinks (Contabo Board API and Format Dashboard Event).

**Outputs added to the item:**
- `taskName` — `[{profile_name}] {jobTitle}`, truncated to 100 chars
- `taskDescription` — multi-section emoji-formatted Markdown:
  - Proposal block (copy-and-send-as-is)
  - Job Snapshot (link, budget, skills, posted)
  - Client Intel (location, rating, total spent, past hires)
  - Routing Info (agent, profile, stack, job ID, generated timestamp UTC)
- `taskBody` — ClickUp-shaped object: `{ name, markdown_description, assignees: [Number(agent_clickup_id)], priority: 2, due_date: now+24h, due_date_time: true, tags: [profile_name, 'vollna-auto'] }`. (No active sink consumes this field as of 2026-04-29; it remains in the item for backward-compat but is dead weight in the payload.)

**Side effect:** Marks `staticData.seenJobs[job.id] = { status: 'success', timestamp: Date.now() }`.

### 5.12 Sinks (HTTP requests, 2-way fan-out)

Both run **in parallel** when `Format ClickUp Task` emits.

| Sink | URL | Auth | Body | Retry / Error |
|---|---|---|---|---|
| **Create Board Task - Self-Hosted** (Contabo) | `http://157.173.110.62/api/v1/webhooks/tasks` | `Authorization: Bearer n8n-board-sync` | Inline custom_fields shape (see §6.2) | `neverError: true`, `onError: continueRegularOutput`, no retry |
| **Format Dashboard Event** → 1 sink (Send to Self-Hosted Dashboard) | `http://157.173.110.62/api/webhook/n8n` | None (HMAC handled at the Dashboard layer if env supports it) | Normalized event shape (see §6.3) | `neverError: true` (no retry, no `onError` set) |

### 5.13 Format Dashboard Event

**Type:** `n8n-nodes-base.code` v2
**Purpose:** Normalize an item from any of the upstream paths (success, no_profile, rejected, gpt_error) into a single dashboard event shape.

**Outcome derivation logic:**
```js
let outcome = item._result || 'unknown';
if (clickupTaskId)                    outcome = 'proposal_created';
else if (item.taskName && item.proposal) outcome = 'proposal_created';
else if (item.error_detail)            outcome = 'gpt_error';
```

So the dashboard receives one of: `proposal_created`, `gpt_error`, `no_profile`, `rejected`, `unknown`. (`weekend`, `inactive`, `duplicate` are never produced today.)

**Note on the success path:** `Format Dashboard Event` is also wired DIRECTLY downstream of `Format ClickUp Task` (one of the four fan-outs). On that path, `$('Format ClickUp Task').item.json` is pulled to get the rich pipeline state, and `clickupTaskId/Url` are always `null` because the ClickUp HTTP response has not arrived yet. This is intentional — the dashboard should not block on ClickUp.

---

## 6. Data contracts

### 6.1 Inbound — Vollna webhook

```jsonc
{
  "filter_name": "Sana",                  // routing fallback when path doesn't match
  "projects": [
    {
      "id": "0123abcdef...",              // OR job_id, uid; OR derived from url
      "title": "Senior Next.js Engineer for SaaS dashboard",
      "description": "...",                // OR snippet, body, job_description
      "url": "https://www.upwork.com/jobs/...?url=https%3A%2F%2F...",
      "budget": "$2,000",                  // OR fixed_price, hourly_budget
      "job_type": "fixed",                 // OR type, budget_type
      "skills": ["React", "Next.js"],     // OR comma-separated string
      "duration": "1-3 months",
      "published": "2026-04-29T12:00:00Z", // OR published_at, created_at, posted_at
      "applicants": 12,                    // OR proposals_count
      "client_details": {
        "country": { "name": "United States" },
        "rating": 4.9,
        "total_spent": 50000,
        "total_hires": 12,
        "payment_method_verified": true
      }
    }
  ]
}
```

The workflow tolerates many variations of these field names because Vollna's payload format has shifted historically — see `Process Job` for the full list of accepted aliases.

### 6.2 Board API payload (`POST /api/v1/webhooks/tasks`)

Sent to `/api/v1/webhooks/tasks` on Contabo (`http://157.173.110.62/api/v1/webhooks/tasks`) with `Authorization: Bearer n8n-board-sync`:

```jsonc
{
  "title": "[Sana] Senior Next.js Engineer for SaaS dashboard",
  "description": "<original Vollna description>",
  "priority": "medium",
  "due_date": "<now+24h ISO8601>",
  "custom_fields": {
    "_job_id": "0123abcdef...",
    "_job_url": "https://www.upwork.com/jobs/~0123abcdef",
    "_budget": "$2,000",
    "_skills": ["React", "Next.js"],
    "_proposal": "<full AI-generated proposal>",
    "_assigned_agent": "Sana",
    "_profile_name": "Sana",
    "_source": "n8n",
    "_client_country": "United States",
    "_client_rating": "4.9",
    "_client_spent": "50000",
    "_client_hires": "12",
    "_stack": "...",
    "_generated": "Apr 29, 2026, 12:00 PM UTC"
  }
}
```

### 6.3 Dashboard webhook payload (`POST /api/webhook/n8n`)

Sent to `/api/webhook/n8n` on Contabo (`http://157.173.110.62/api/webhook/n8n`). Shape produced by `Format Dashboard Event`:

```jsonc
{
  "event": "job_processed",
  "timestamp": "2026-04-29T12:00:00.000Z",
  "outcome": "proposal_created",         // OR gpt_error | no_profile | rejected | unknown
  "reason": "",                            // populated on non-success paths

  "job": {
    "id": "0123abcdef",
    "title": "...", "url": "...",
    "budget": "...", "budgetType": "...",
    "skills": [...], "duration": "...",
    "postedDate": "...",
    "applicants": 0,
    "description": "<first 500 chars>"
  },

  "client": {
    "country": "...", "rating": "...",
    "spent": "...", "hires": "...",
    "paymentVerified": false
  },

  "routing": {
    "filterName": "Sana", "profileName": "Sana",
    "assignedAgent": "...", "agentClickupId": "..."
  },

  "scores": {
    "heuristic": null, "heuristicFlags": [],
    "aiScore": null, "aiRelevant": null,
    "aiReason": "", "aiHighlights": [], "aiRedFlags": []
  },

  "proposal": "<full proposal text>",
  "clickupTaskName": "[Sana] ...",

  "clickup": {
    "taskId": null,                        // always null on the new flow
    "taskUrl": null
  }
}
```

The Dashboard's `/api/webhook/n8n` route normalizes this further before writing to `jobs`. Non-`proposal_created` outcomes are gracefully skipped (no insert) rather than rejected — see CLAUDE.md "Non-proposal outcome handling".

---

## 7. Routing & profile mapping

The single source of truth for which Vollna `filter_name` belongs to which agent is the dashboard endpoint **`GET /api/profiles/mapping`**:

```jsonc
{
  "Sana":    { "profile_id": "...", "stack": "...", "assigned_agent": "...", "agent_clickup_id": "...", "clickup_list_id": "..." },
  "Laiba":   { ... },
  // ... one entry per active profile
}
```

`Process Job` fetches this on **every execution** (no n8n-side cache). Contabo is primary, Vercel is fallback. Admin assignment changes from the Settings UI propagate to the next inbound webhook.

**Path resolution priority in `Process Job`:**
1. `headers.path` ⊇ `<lowercase-name>-profile-webhook` → match (works for Rebekah/Nawal).
2. Fall back to `body.filter_name || body.filterName || body.filter?.name || body.filter_tag || body.filter` → works for the 6 UUID-style webhooks because Vollna sends `filter_name` in the body.

**Adding a new agent profile** is documented in CLAUDE.md ("Adding a New Profile/Webhook Node to n8n"). At the workflow level it requires: addNode (Webhook + Respond), addConnection (Webhook → Respond, Respond → Merge with `targetIndex = N`), updateNode (`Merge All Webhooks.parameters.numberInputs += 1`).

---

## 8. Hook rotation & system prompt (AI Agent)

### 8.1 Three hook types

| Hook | Style | Formula | Max length |
|---|---|---|---|
| **A** | Curiosity-led | `"Would you like to see [PROBLEM]? + [I can show on a screen share]"` | 200 chars |
| **B** | Prototype hook | `"I have a working prototype that already [PROBLEM] — I can show you on a call."` | 200 chars |
| **C** | Psychological question | A psychologically sharp founder-friendly question that exposes hidden risk | 200 chars |

### 8.2 Rotation today

`Build GPT Input` only rotates between **A** and **B** per profile. Hook **C** is defined in the system prompt (the agent must print all three at the top of every response) but is never selected by `USE HOOK:`. The user-visible proposal always uses A or B in Section 1.

### 8.3 Output formatting requirement

The agent prints `--- Hook A ---`, `--- Hook B ---`, `--- Hook C ---` blocks at the top, then the full proposal below. Agents copy the proposal section only into Upwork.

---

## 9. Configuration & secrets

| Item | Where | Notes |
|---|---|---|
| **Anthropic credential** | n8n credentials, id `fVtEWZhGXzEBZDoS` ("Aleem Anthropic account") | Used by `Claude Chat Model - Proposal` |
| **ClickUp credential** | n8n credentials, id `0M8vRzxHZsZjgRc3` ("Ikonicdev") | UNUSED as of 2026-04-29 (legacy `Create ClickUp Task` sink removed). Safe to delete after a 1-week observation window. |
| **Board API bearer token** | Hard-coded in HTTP nodes: `Bearer n8n-board-sync` | Matches the Dashboard's `webhook_configs` row provisioned in migration 008 |
| **Default ClickUp list** | `Process Job` const `DEFAULT_LIST = '901815302064'` | Used when a profile has no `clickup_list_id` set |
| **Profile mapping URLs** | Hard-coded in `Process Job` (Contabo primary, Vercel fallback) | Vercel fallback retained pending separate decision (see §13). If endpoints change, code must be patched |
| **Dashboard URL** | Hard-coded in `Send to Self-Hosted Dashboard` (`http://157.173.110.62/api/webhook/n8n`) | No env-var indirection |
| **Workflow timezone** | `settings.timezone: Asia/Karachi` | All `new Date()` and any time-dependent code operates in PKT |

---

## 10. Operational requirements

### 10.1 Latency budget

| Phase | Typical | Notes |
|---|---|---|
| Webhook → Respond | <100ms | Vollna's connection closes here |
| Process Job (incl. profile fetch) | 100–500ms | Network-bound by `/api/profiles/mapping` |
| AI Agent (Claude Haiku 4.5, temp 0.2) | 5–20s | Claude p99 is ~30s; Haiku is the fastest tier |
| Format + 4 sinks (parallel) | 200–800ms | Bound by slowest sink (usually ClickUp or Vercel) |
| **End-to-end success path** | **6–22s** | |

### 10.2 Concurrency

n8n cloud runs each webhook trigger in its own execution. The Merge node v3.2 with 8 unique inputs ensures concurrent agent jobs are isolated.

### 10.3 Active hours

The workflow processes jobs 24/7 including weekends. Vollna is the gating mechanism: agents pause/resume their own Vollna feeds outside working hours. (A `Check Active Hours` node previously existed but was removed on 2026-04-29 — see §12.)

### 10.4 Retry policy & failure modes

| Failure | Behavior |
|---|---|
| Vollna POST hits an n8n save-time blip (~1–2s during a workflow update) | Webhook returns 404; **lost event** (Vollna does not retry) |
| `Process Job` fails to fetch profile mapping (both Contabo + Vercel down) | Emits `_result: 'rejected'` → goes to Format Dashboard Event → both dashboards record a `rejected` outcome |
| Claude API error / malformed output | Output parser autoFix attempts repair; if `_proposalOk: false` → Extract Error path → `gpt_error` outcome |
| Contabo Board API down | `neverError: true` swallows the error; **no retry**, no failover |
| Contabo Dashboard webhook down | Same — swallowed, no retry |

**Note:** Neither active sink retries — both rely on `neverError: true`. A transient 503 on the Dashboard or Board API loses that single event with no record. With the dual-environment safety net retired (Vercel sinks removed 2026-04-29), this risk is now first-order. See TD-5 / TD-8 / TD-10.

### 10.5 Rollback procedure

The committed snapshot at `docs/multiple webhooks (working flow).json` is the source-of-truth pre-2026-04-29 state. Plus n8n's internal versioning (`mcp__n8n-mcp__n8n_workflow_versions`).

**To rollback the Merge index fix:**
```
mcp__n8n-mcp__n8n_update_partial_workflow with operations:
  - removeConnection: Respond - Laiba → Merge All Webhooks  (and Khansa, Saim, Shayan, Craig, Rebekah)
  - addConnection: Respond - Laiba → Merge All Webhooks (no targetIndex, defaults to 0)
  - (repeat for the other 5)
```

---

## 11. Known issues & technical debt

| ID | Issue | Severity | Notes |
|---|---|---|---|
| **TD-1** | ~~`Check Active Hours` is disconnected~~ **RESOLVED 2026-04-29** | — | Node deleted entirely; active-hours gating is no longer enforced at the workflow level. See §12. |
| **TD-2** | Hook C is defined but never used | Low | `Build GPT Input` rotates A↔B only. Either extend to A/B/C rotation OR drop Hook C from the system prompt. |
| **TD-3** | Mixed webhook path styles (UUID vs slug) | Low | First 6 agents use UUID paths and rely on `body.filter_name` for routing. CLAUDE.md doc previously implied uniform slug paths. The "Adding a New Profile" blueprint already uses the slug style for new agents — divergence will resolve over time. |
| **TD-4** | Dead Switch rules (`inactive`, `duplicate`, `weekend`) and unconnected fallback | Low | Cosmetic. Removing them would clean up the Route Job UI. |
| **TD-5** | Retry asymmetry across sinks | Medium | Only ClickUp retries. Add `retryOnFail: true, maxTries: 3, waitBetweenTries: 2000` to the four active HTTP nodes (Vercel/Contabo Board APIs + Vercel/Contabo Dashboard sinks). |
| **TD-6** | Outdated typeVersions | Low | Route Job (3 vs 3.4), Proposal OK? (2 vs 2.3), 8x Respond (1.1 vs 1.5). Bumping these is low risk but should be done in one batch. |
| **TD-7** | ~~`Create ClickUp Task` still wired~~ **RESOLVED 2026-04-29** | — | Sink + inbound edge deleted. Fan-out from `Format ClickUp Task` is now 3-way. ClickUp credential `0M8vRzxHZsZjgRc3` is unused but not yet deleted. See §12. |
| **TD-8** | No record of dropped events on the four `neverError: true` sinks | Medium | Combined with TD-5, a transient blip silently loses leads. Consider adding a tiny `On Error → Log to Dashboard` branch from each sink. |
| **TD-9** | `Process Job` profile-mapping fetch happens on every execution (no caching) | Low | Adds 100–500ms per event. Acceptable today; revisit if traffic grows. |
| **TD-10** | Single-environment failure mode | Medium | The Vercel parallel sinks were removed on 2026-04-29; the dual-environment safety net is gone. A Contabo outage now means lost leads (no failover). Mitigation: implement TD-5 (retry) and TD-8 (DLQ) so a transient blip does not silently drop events. |

---

## 12. Recent changes

### 2026-05-12 — Create Board Task - Self-Hosted: added `_relevancy_summary` (12th relevancy field) (SHIPPED)
- **What:** Added one key/value to `Create Board Task - Self-Hosted.parameters.jsonBody` (node id `a55e6692-843c-4ef6-ba9b-e62a3ddab7b6`) — `_relevancy_summary: ($json.relevancyVerdict && $json.relevancyVerdict.summary) || null`. Inserted directly after `_relevancy_score` in the `custom_fields` object literal. Brings the total `_relevancy_*` stamp from 11 fields to 12.
- **Why:** User feedback (diagnosed via execution 13387): the classifier verdict has a top-level `summary` field carrying the LLM's qualitative feedback text (e.g. "Strong skill match on React/Node; client spend in target range; portfolio aligns") that was not being captured on cards. Needed for Shadow-mode calibration so reviewers can read the LLM's reasoning alongside score/tier/decision.
- **How:** Single atomic `n8n_update_partial_workflow` call, 1 `patchNodeField` op on `Create Board Task - Self-Hosted`, `fieldPath: parameters.jsonBody`. Defensive `($json.relevancyVerdict && $json.relevancyVerdict.summary) || null` pattern matches the other 11 fields (n8n expression engine doesn't support `?.`). No other node touched.
- **Verification:** `n8n_validate_workflow profile=runtime` → `valid: true, errorCount: 0, warningCount: 48` (unchanged from prior post-Build-GPT-Input-patch baseline). Node count still 34. Re-read of live workflow confirms exactly one occurrence of `_relevancy_summary: ($json.relevancyVerdict && $json.relevancyVerdict.summary) || null` in the node's jsonBody.
- **Live verification pending:** awaits next real Vollna fire. Acceptance: `SELECT custom_fields->>'_relevancy_summary' FROM tasks WHERE created_at > NOW() - INTERVAL '5 min'` returns the LLM's qualitative feedback text (non-null when classifier ran in Active or Shadow mode; null on K1-bypass / classifier-error fallback paths).
- **Rollback baseline:** `docs/multiple webhooks (working flow).json` (refreshed post-patch).
- **Rollback op (single inverse `patchNodeField`):**
  ```json
  {
    "type": "patchNodeField",
    "nodeName": "Create Board Task - Self-Hosted",
    "fieldPath": "parameters.jsonBody",
    "patches": [{
      "find": "_relevancy_score: ($json.relevancyVerdict && $json.relevancyVerdict.total_score) || null, _relevancy_summary: ($json.relevancyVerdict && $json.relevancyVerdict.summary) || null, _relevancy_decision:",
      "replace": "_relevancy_score: ($json.relevancyVerdict && $json.relevancyVerdict.total_score) || null, _relevancy_decision:"
    }]
  }
  ```

### 2026-05-12 — Build GPT Input source patch: read from Route Job, not $input (SHIPPED)
- **What:** Patched `Build GPT Input` (node id `e21fa42e-9d56-43fe-a253-8c83849d4d07`) `parameters.jsCode`. Changed one line: `const item = $input.item.json;` → `const item = $('Route Job').item.json;`. All other lines (comments, hook rotation logic, gptInput template, `return [{ json: { ...item, gptInput } }]`) unchanged.
- **Why:** The Phase 7 splice introduced `executeWorkflow` (K2 `Score Relevancy`) into the active path. n8n's `executeWorkflow` replaces the current item with the sub-workflow's output, so `$input.item.json` at `Build GPT Input` now resolves to the classifier verdict object (`{ decision, effective_decision, job_external_id, ... }`) instead of the original Process Job payload that contains `job.budget`, `job.title`, `profile_name`, etc. Result: `Cannot read properties of undefined (reading 'budget')` on line 9. Diagnosed via live execution 13379 (Khansa profile, classifier hit DLQ fallback, C12 synthesized a "review" verdict, K3 correctly routed to active_review, but `Build GPT Input` then threw on `.budget`).
- **Fix mechanics:** `$('Route Job').item.json` uses n8n's paired-item tracking — when Vollna sends multi-job webhooks (e.g. execution 13378 had 2 jobs), each verdict item is correctly paired back to its source Route Job item. `Route Job` is upstream of all four paths that lead into Build GPT Input (K1 bypass, K3 active_proceed, K3 active_review, K3 shadow_any), so it's always executed. Downstream behavior unchanged: `return [{ json: { ...item, gptInput } }]` still spreads Route Job's payload + the new `gptInput`. Format ClickUp Task continues to read the classifier verdict via `$('Score Relevancy').isExecuted` — that still works because Score Relevancy remains an executed upstream node when the classifier runs.
- **How:** Single atomic `n8n_update_partial_workflow` call, 1 `patchNodeField` op on Build GPT Input. No topology change, no other node touched.
- **Verification:** `n8n_validate_workflow profile=runtime` → `valid: true, errorCount: 0, warningCount: 48` (one new heuristic warning "Code doesn't reference input data" on Build GPT Input — false positive: the static analyzer doesn't recognize `$('NodeName').item.json` as input data). Refetched jsCode confirms the exact 1-line diff. Node count still 34.
- **Restores:** Shadow mode's no-routing-change contract — every job drafts a proposal regardless of whether the classifier verdict was real, a DLQ fallback, or a K1 bypass.
- **Rollback baseline:** `docs/multiple webhooks (working flow).json` (refreshed post-patch).
- **Rollback op (single inverse `patchNodeField` if we ever need to revert):**
  ```json
  {
    "type": "patchNodeField",
    "nodeName": "Build GPT Input",
    "fieldPath": "parameters.jsCode",
    "patches": [{ "find": "const item = $('Route Job').item.json;", "replace": "const item = $input.item.json;" }]
  }
  ```
- **Live verification pending:** awaits next real Vollna fire. Acceptance: Build GPT Input completes; path traverses Build GPT Input → AI Agent - Proposal Writer → Merge Proposal with Job Data → Proposal OK? → Format ClickUp Task → Create Board Task - Self-Hosted; card lands on board (in Todo) with `_relevancy_*` custom_fields populated; works for both real classifier verdicts and DLQ fallbacks.

### 2026-05-12 — K1 condition patch #2: hardcoded literal (no $env, no expression) (SHIPPED)
- **What:** Replaced `IF - Classifier Enabled` (K1, node id `f95daa14-6a69-49ab-a8c1-f4428c785685`) `parameters.conditions` with a fully literal string-comparison form. New shape:
  - `leftValue`: `"enabled"` (literal STRING — no `={{ }}` template, no expression evaluator involved)
  - `rightValue`: `"disabled"` (literal string)
  - `operator`: `{ type: "string", operation: "notEquals" }`
  - `options.typeValidation`: `"strict"` (kept; trivially satisfied for string types)
  - `combinator`: `"and"`
- **Why:** This morning's first K1 patch (string-comparison form with `leftValue: ={{ $env.RELEVANCY_CLASSIFIER_ENABLED || 'true' }}`) STILL fired output[1] for every item — confirmed in live execution 13378 (Saim profile, 2 jobs, both bypassed; Score Relevancy never ran). Root cause: `$env.RELEVANCY_CLASSIFIER_ENABLED` on this n8n cloud plan tier doesn't behave like documented — even with `|| 'true'` fallback, the property access breaks IF condition evaluation (not just returns undefined). CLAUDE.md's previous gotcha ("$env.MY_VAR resolves to undefined") was incomplete: in practice the access also breaks expressions inside IF nodes.
- **Semantics:** `'enabled' !== 'disabled'` is always `true` → K1 always fires output[0] → Score Relevancy always runs. The classifier kill switch is now a **manual K1 node edit**: flip `leftValue` from `"enabled"` to `"disabled"` to bypass. Shadow vs Active mode toggle is independent and still works via `system_settings.relevancy.classifier_mode` (consumed downstream in K3 / Route Verdict).
- **How:** Single atomic `n8n_update_partial_workflow` call, 1 `updateNode` op replacing `parameters.conditions`. No topology change, no other node touched.
- **Verification:** `n8n_validate_workflow profile=runtime` → `valid: true, errorCount: 0, warningCount: 47` (unchanged from prior baseline). `n8n_get_workflow` → still 34 nodes; K1 re-read confirms `leftValue` is the literal `"enabled"` (no expression wrapper).
- **Rollback baseline:** `docs/multiple webhooks (working flow).json` (refreshed post-patch).
- **Rollback op (single `updateNode` to restore the previous string-form expression):**
  ```json
  {
    "type": "updateNode",
    "nodeName": "IF - Classifier Enabled",
    "updates": {
      "parameters.conditions": {
        "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "strict", "version": 2 },
        "conditions": [{
          "id": "classifier-enabled-1",
          "leftValue": "={{ $env.RELEVANCY_CLASSIFIER_ENABLED || 'true' }}",
          "rightValue": "false",
          "operator": { "type": "string", "operation": "notEquals" }
        }],
        "combinator": "and"
      }
    }
  }
  ```
- **Live verification pending:** awaits next real Vollna fire. Acceptance: K1 fires output[0] → Score Relevancy runs → Route Verdict routes per mode. For Shadow-mode jobs: Build GPT Input runs as before, card lands in Todo with `_relevancy_*` custom_fields populated, new row in `relevancy_scores`.

### 2026-05-12 — K1 condition patch: switch to string operator (SHIPPED)
- **What:** Replaced `IF - Classifier Enabled` (K1, node id `f95daa14-6a69-49ab-a8c1-f4428c785685`) `parameters.conditions` with a string-comparison form. New shape:
  - `leftValue`: `={{ $env.RELEVANCY_CLASSIFIER_ENABLED || 'true' }}` (interpolates to string)
  - `rightValue`: `"false"` (string literal)
  - `operator`: `{ type: "string", operation: "notEquals" }`
  - `options.typeValidation`: `"strict"` (kept — trivially satisfied for string types)
  - `combinator`: `"and"`
- **Why:** The previous form used `={{ ($env.RELEVANCY_CLASSIFIER_ENABLED || 'true') !== 'false' }}` with operator `{ type: "boolean", operation: "true", singleValue: true }`. n8n's `={{ }}` template interpolation stringifies the boolean result; with `typeValidation: "strict"` the stringified `"true"` failed the strict-boolean type check, so K1 always fired output[1] (false → bypass classifier). Discovered live in execution 13375: every job went straight from Route Job → K1 → Build GPT Input, skipping Score Relevancy + Route Verdict entirely (the Phase 7 splice was effectively dead code).
- **Semantics:** K1 fires output[0] (true → classifier) when `$env.RELEVANCY_CLASSIFIER_ENABLED` is unset, empty, or anything other than the literal string `"false"`. Fires output[1] (bypass) ONLY when explicitly set to `"false"`. Default-on behavior preserved. On n8n cloud (this plan tier) `$env.MY_VAR` resolves to `undefined`; `undefined || 'true'` → `'true'`; `'true' !== 'false'` → output[0]. Correct.
- **How:** Single atomic `n8n_update_partial_workflow` call, 1 `updateNode` op replacing `parameters.conditions`. No topology change, no other node touched.
- **Verification:** `n8n_validate_workflow profile=runtime` → `valid: true, errorCount: 0, warningCount: 47` (unchanged from post-splice baseline). `n8n_get_workflow mode=structure` → still 34 nodes, all classifier wiring (Route Job → K1 → Score Relevancy → Route Verdict → {Build GPT Input | End}) intact. K1 re-read confirms the new operator + leftValue + rightValue.
- **Rollback baseline:** `docs/multiple webhooks (working flow).json` (refreshed post-patch).
- **Rollback op (single `updateNode` to restore prior parameters):**
  ```json
  {
    "type": "updateNode",
    "nodeName": "IF - Classifier Enabled",
    "updates": {
      "parameters.conditions": {
        "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "strict", "version": 2 },
        "conditions": [{
          "id": "classifier-enabled-1",
          "leftValue": "={{ ($env.RELEVANCY_CLASSIFIER_ENABLED || 'true') !== 'false' }}",
          "rightValue": true,
          "operator": { "type": "boolean", "operation": "true", "singleValue": true }
        }],
        "combinator": "and"
      }
    }
  }
  ```
- **Live verification pending:** awaits the next real Vollna webhook fire. Acceptance — execution path now traverses Score Relevancy + Route Verdict; for Shadow-mode + Shayan profile, K3 fires output[3] (`shadow_any`) → Build GPT Input → proposal drafted → Todo card with `_relevancy_*` fields populated; new row in `relevancy_scores` with `classifier_mode_at_decision='shadow'`.

### 2026-05-12 — Phase 7 SPLICED: relevancy classifier wired into parent in Shadow mode (SHIPPED)
- **What:**
  - Added 4 new nodes: `IF - Classifier Enabled` (kill-switch), `Score Relevancy` (executeWorkflow → `hi71jhPU8tmq7hEp`), `Route Verdict` (Switch v3.2 keyed on `effective_decision + classifier_mode`), `End (Audit Only)` (noOp terminal for active-reject + classifier-error paths).
  - Spliced between **Route Job output[0] (the `_result: proceed` branch) and Build GPT Input** — NOT between Process Job and Route Job. This preserves the existing `no_profile` / `rejected` audit paths through Format Dashboard Event untouched.
  - Edited `Format ClickUp Task.jsCode`: surgical insertion to pull `$('Score Relevancy').first().json` when executed and expose it as top-level `relevancyVerdict` on the returned item (wrapped in try/catch so the K1 false-branch bypass path stays harmless).
  - Edited `Create Board Task - Self-Hosted.jsonBody`: spread 11 `_relevancy_*` fields into `custom_fields` (`_relevancy_score`, `_decision`, `_effective`, `_threshold_flipped`, `_reasons`, `_tier`, `_confidence`, `_score_id`, `_dlq_id`, `_evaluated_at`, `_mode_at_decision`). All lookups use the defensive `($json.relevancyVerdict && $json.relevancyVerdict.<key>) || default` pattern to be safe on the K1 false-branch where `relevancyVerdict` is `null`.
  - Activated sub-workflow `hi71jhPU8tmq7hEp` (required by n8n cloud's executeWorkflow validator — the platform rejects publishing a parent that references an unpublished sub-workflow, regardless of intended Shadow-mode behavior).
  - 30 → **34 nodes**.
- **Why:** Closes P1 from §13. Plan v3.3 Phase 7. Shadow mode by default (`system_settings.relevancy.classifier_mode='shadow'` from migration 018) means every job is scored and audited but the existing Build GPT Input → AI Agent → Proposal path runs unchanged. Operator flips to Active via the Settings UI (not from this workflow).
- **Deviation from plan v3.3 §4.4.6 op 6:** The plan's literal instruction is "remove Process Job → Route Job, splice K1/K2/K3/K4 in between." That would route `_result: no_profile` and `_result: rejected` payloads (which have no `profile_id` and no `job` object) into the classifier where they'd 404 in C1 (Load Profile Context) and fall through to K4, silently breaking the Dashboard webhook's audit visibility for those outcomes. User-approved alternate path (recorded 2026-05-12): splice only on Route Job output[0]. Process Job → Route Job and Route Job outputs[1..4] → Format Dashboard Event stay untouched.
- **Deviation from plan v3.3 §4.4.2:** Plan's literal instruction was to set `custom_fields._relevancy_*` inside `Format ClickUp Task.jsCode` — but that node has no `custom_fields` variable. Real `custom_fields` payload is built inside the `Create Board Task - Self-Hosted.jsonBody` expression. Two-node atomic edit (FCT exposes `relevancyVerdict`; CBT spreads it) was chosen for correctness.
- **NOT changed:** Process Job jsCode, Route Job config/rules, AI Agent - Proposal Writer (model + system prompt), Format Dashboard Event, Send to Self-Hosted Dashboard, all 8 Webhook + Respond + Merge wiring (all indices 0..7 preserved, `numberInputs: 8` unchanged). Column literal anywhere — not edited (the n8n cards already land in `Todo` via `/api/v1/webhooks/tasks` route.ts:67-76 `columns[0]` fallback; plan v3.3 §4.4.2's "change column to Todo" turned out to describe a non-existent problem after pre-flight DB inspection by the prior workflow-keeper instance).
- **How:** Single atomic `n8n_update_partial_workflow` call, 16 operations: 4× `addNode` + 1× `removeConnection` + 9× `addConnection` + 2× `patchNodeField`. Sub-workflow activation was a separate prior call (`activateWorkflow` on `hi71jhPU8tmq7hEp`) because the parent splice validator requires the sub published first.
- **Verification:** `n8n_validate_workflow profile=runtime` → `valid: true, errorCount: 0, warningCount: 47` (up from ~43 baseline; all 4 new warnings are stylistic — outdated typeVersion on the two new switch/exec nodes plus error-output-handling advisories on IF/Switch nodes whose second output is treated as the "false/reject" branch). **Zero new errors.** 16/16 operations applied. Sub-workflow C12 output JSON keys (verified by classifier-keeper earlier) align with all splice-critical paths: `effective_decision`, `request_meta.classifier_mode`, `total_score`, `decision`, `threshold_flipped`, `rejection_reasons`, `tier`, `confidence`, `_score_id`, `_dlq_id`.
- **Rollback baseline:** `docs/multiple webhooks (working flow).json` (refreshed post-splice, 34 nodes). Pre-splice snapshot is committed at the prior PRD entry's commit. Use `mcp__n8n-mcp__n8n_workflow_versions` for cloud-side history if a faster rollback is needed.
- **Rollback ops (inverse of this run, single atomic partial-update):**
  ```
  - removeConnection × 9 (all new edges from K-nodes)
  - removeConnection: Route Job → IF - Classifier Enabled (sourceIndex=0)
  - addConnection: Route Job → Build GPT Input (sourceIndex=0, targetIndex=0)
  - removeNode × 4 (End (Audit Only), Route Verdict, Score Relevancy, IF - Classifier Enabled)
  - patchNodeField "Format ClickUp Task" — reverse the two patches (remove relevancyVerdict try/catch + remove `relevancyVerdict,` from return)
  - patchNodeField "Create Board Task - Self-Hosted" — reverse the jsonBody patch (strip the 11 `_relevancy_*` fields from custom_fields)
  - separately: n8n_update_partial_workflow on hi71jhPU8tmq7hEp with deactivateWorkflow (only if rolling back the activation too)
  ```
- **Live verification pending:** awaits next real Vollna webhook fire OR user-triggered test. Acceptance: card lands in Todo with `_relevancy_*` custom_fields populated; new row in `relevancy_scores` with `classifier_mode_at_decision='shadow'`; existing Claude proposal still drafts (Shadow promise — no routing change).
- **TD-10 / TD-5 / TD-8 still open** — relevancy classifier adds a new sink (`Score Relevancy`) but it has `retryOnFail: true, maxTries: 2, waitBetweenTries: 1500` configured and routes errors to K4. The existing 2 active HTTP sinks (Create Board Task / Send to Dashboard) still rely on `neverError: true` without retry — unchanged in this run.

### 2026-05-11 — Relevancy classifier sub-workflow created (NOT YET SPLICED)
- **What:** New sub-workflow `_relevancy-classifier-core` (id `hi71jhPU8tmq7hEp`) built and validated. 14 nodes, end-to-end smoke-tested (Shayan + synthetic SaaS job → `proceed/91/apply_now` in 18.2s). Currently **inactive** — no path in this parent workflow invokes it yet.
- **What does NOT change in this parent workflow:** No edits to `EWnZg3svZWwcIRs4`. `Process Job → Build GPT Input → AI Agent (Proposal Writer)` still runs unchanged. The classifier is built but quarantined.
- **Why:** Phases 6A + 6B of plan v3.3 split the rollout: build + smoke-test the classifier first (this milestone); splice into the parent workflow as a separate Phase 7 to keep blast radius minimal and reversal trivial.
- **What ships in Phase 7** (per plan v3.3 §4.2 / §4.4.1): a kill-switch IF (`$env.RELEVANCY_CLASSIFIER_ENABLED`), a `Score Relevancy` executeWorkflow node calling `hi71jhPU8tmq7hEp`, a `Route Verdict` switch keyed on `effective_decision + classifier_mode`, an `End (Audit Only)` noOp for active-reject branches, and a one-line edit to `Format ClickUp Task` setting `column: "Todo"` (today it writes `"Proposal Submitted"` which is operationally misleading — a Todo card is "drafted, needs human to paste + submit on Upwork"; Proposal Submitted is "live on Upwork awaiting client response", human-only destination).
- **Why ship in Shadow mode by default** (plan v3.3 §10.6.1): the new system_settings seed has `relevancy.classifier_mode='shadow'`, so even after the parent splice, the existing proposal writer + Todo-card creation runs as today. Shadow mode logs verdicts but does NOT change routing. Flipping to Active is a Settings-UI operator action.
- **Sub-workflow PRD:** `docs/n8n_relevancy_classifier_core_prd.md`.

### 2026-04-29 — Vercel sinks retired; workflow now writes only to Contabo (SHIPPED)
- **What:**
  - Deleted `Create Board Task` (Vercel) and its inbound edge from `Format ClickUp Task`. Fan-out from `Format ClickUp Task` is now 2-way: `Create Board Task - Self-Hosted` (Contabo) + `Format Dashboard Event`.
  - Deleted `Send to Dashboard` (Vercel) and its inbound edge from `Format Dashboard Event`. Only `Send to Self-Hosted Dashboard` remains.
  - 32 → 30 nodes.
- **Why:** User has retired the Vercel deployment and consolidated all writes on the Contabo self-hosted server (`http://157.173.110.62`). The dual-write design is no longer needed.
- **How:** Single atomic `n8n_update_partial_workflow` call with two `removeNode` operations (the partial-update tool auto-cleans the inbound connections — no separate `removeConnection` ops needed).
- **Verification:** `n8n_validate_workflow profile=runtime` reports `valid: true`, 0 errors, 42 warnings (down from 43 — one HTTP-no-error-handling warning gone with the deleted Vercel sinks). All 8 Respond → Merge target indices preserved (Sana=0..Nawal=7); `numberInputs: 8` unchanged. `Format ClickUp Task` outbound edges = 2; `Format Dashboard Event` outbound edges = 1.
- **Rollback baseline:** `docs/multiple webhooks (working flow).json` (refreshed post-change, versionId `e251bf20-e3bf-4dfd-b8d0-00ba4e156f39`). Pre-change snapshot is committed at `e843c4a`. Use `mcp__n8n-mcp__n8n_workflow_versions` for cloud-side history.
- **NOT changed in this run (flagged as follow-up):** `Process Job`'s profile-mapping fetch still has Vercel as a fallback URL. The user may keep it as a temporary safety net or remove it once the Vercel deployment is fully torn down. See §13.
- **New tech-debt:** TD-10 (Medium) — single-environment failure mode now exists. Mitigation = TD-5 (retry) + TD-8 (DLQ).

### 2026-04-29 — TD-7 + TD-1 cleanup: removed `Create ClickUp Task` and `Check Active Hours` nodes (SHIPPED)
- **What:**
  - Deleted `Create ClickUp Task` node (legacy ClickUp sink) and its inbound edge from `Format ClickUp Task`. Fan-out is now 3-way: `Create Board Task` (Vercel) + `Create Board Task - Self-Hosted` (Contabo) + `Format Dashboard Event`.
  - Deleted `Check Active Hours` node entirely. The workflow no longer enforces a business-hours / weekend gate.
- **Why:** TD-7 (ClickUp killed in M8 — sink was dead weight). TD-1 (CAH had been orphaned since pre-2026-04-29; cleanup confirmed it would not be re-enabled).
- **How:** Two atomic `n8n_update_partial_workflow` calls.
  - **Run 1 (TD-7):** removeConnection `Format ClickUp Task` → `Create ClickUp Task`; removeNode `Create ClickUp Task`. The MCP partial-update validator rejected the save while CAH was orphaned, so the same call also spliced CAH back into the path as `disabled: true` (`Merge → CAH (disabled) → Process Job`). Disabled nodes pass data through unchanged, so runtime behaviour was unchanged.
  - **Run 2 (TD-1):** removeConnection `Merge All Webhooks` → `Check Active Hours`; removeConnection `Check Active Hours` → `Process Job`; removeNode `Check Active Hours`; addConnection `Merge All Webhooks` → `Process Job` (restored direct edge).
- **Verification:** `n8n_validate_workflow profile=runtime` after both runs reports `valid: true`, 0 errors, 43 pre-existing warnings (no new ones; the prior "Connection to disabled node" warning is gone). All 8 Respond → Merge target indices preserved (Sana=0..Nawal=7); `numberInputs: 8` unchanged. Live: jobs continued flowing to the Task Board throughout.
- **Rollback baseline:** `docs/multiple webhooks (working flow).json` (refreshed post-change). For prior-state restore use `mcp__n8n-mcp__n8n_workflow_versions`.
- **Follow-up:** ClickUp credential `0M8vRzxHZsZjgRc3` is now unused; safe to delete after a 1-week observation window.

### 2026-04-29 — Merge index restoration (SHIPPED)
- **What:** Re-wired Respond - Laiba/Khansa/Saim/Shayan/Craig/Rebekah → Merge All Webhooks with unique target indices 1–6 respectively. Sana=0 and Nawal=7 were already correct.
- **Why:** 7 of 8 Respond nodes had collided on input 0 (regression of unknown origin). Per-agent isolation was no longer guaranteed.
- **How:** Single atomic `n8n_update_partial_workflow` call, 12 ops (6 × removeConnection + 6 × addConnection with `targetIndex`).
- **Verification:** Live test — Vollna firing real jobs after the change; tasks landed on the Task Board for each agent without collision.
- **Rollback baseline:** `docs/multiple webhooks (working flow).json`.

### 2026-04-14 — `Process Job` switched to Contabo-primary mapping fetch
- See `memory/n8n_multiple_webhooks_workflow.md`.

### 2026-04-11 — `Format Dashboard Event` repositioned to fan out to Vercel + Contabo
- Both dashboards now receive every event in parallel.

---

## 13. Future improvements (proposed, not committed)

| Priority | Idea | Effort |
|---|---|---|
| ~~**P1**~~ ✅ **DONE 2026-05-12** | ~~**Phase 7**: splice `_relevancy-classifier-core` into this workflow. Adds 4 nodes (kill-switch IF, Score Relevancy executeWorkflow, Route Verdict switch, End audit-only noOp) + edits `Format ClickUp Task.column` from "Proposal Submitted" → "Todo". Shadow-mode by default.~~ See §12 entry "2026-05-12 — Phase 7 SPLICED" for the as-shipped details (includes two user-approved deviations from plan v3.3). | — |
| P1 | Add retry to the 2 active sinks (TD-5 / TD-10) | 1 partial-update call, ~5 min — first-order priority since Vercel parallel was retired |
| P2 | Decide on `Process Job` Vercel fallback for `/api/profiles/mapping` (keep as safety net OR remove once Vercel is fully retired) | <5 min via `patchNodeField` |
| P2 | Wire Hook C into rotation OR remove from system prompt (TD-2) | Code-node patch, ~10 min |
| P2 | Migrate the 6 UUID-style webhooks to slug paths (TD-3) | Coordinated change with Vollna; not urgent |
| P3 | Replace 4 sink `neverError: true` with `onError + capture` to a dead-letter sink (TD-8) | 30–60 min, needs a DLQ endpoint |
| P3 | Bump outdated typeVersions in one maintenance batch (TD-6) | 15 min |
| P3 | Delete unused ClickUp credential `0M8vRzxHZsZjgRc3` from n8n | <1 min, after observation window |
| P4 | Trim dead Switch rules `inactive`/`duplicate`/`weekend` (TD-4) | 5 min, cosmetic |

---

## 14. Glossary

| Term | Meaning |
|---|---|
| **Vollna** | External Upwork-feed scraper that posts matched jobs to per-agent webhooks |
| **Profile** | A named Vollna filter / Upwork persona owned by one agent (e.g. "Sana", "Nawal") |
| **Filter name** | The string Vollna sends to identify which profile the job is for; the workflow uses it to look up the agent |
| **Hook** | The opening 1–2 sentences of the proposal designed to maximize open/response rate |
| **Outcome** | The terminal label of a workflow execution from the dashboard's perspective: `proposal_created`, `gpt_error`, `no_profile`, `rejected`, `unknown` |
| **Sink** | An outbound HTTP destination (ClickUp API, Board API, Dashboard webhook) |
| **Board API** | `POST /api/v1/webhooks/tasks` — creates a Task Board task |
| **Dashboard webhook** | `POST /api/webhook/n8n` — creates a `jobs` row, auto-assigns an agent, optionally creates a board task with `_source: n8n` |
| **PKT** | Asia/Karachi timezone (UTC+5) |

---

## 15. References

- `CLAUDE.md` — project-level context, operational gotchas, migration history
- `docs/multiple webhooks (working flow).json` — committed snapshot of the live workflow (current as of 2026-04-29)
- `docs/taskboard_prd.md` — Task Board product requirements
- `memory/n8n_multiple_webhooks_workflow.md` — auto-memory: topology + recent changes
- n8n cloud workflow URL: `https://ikonicdev.app.n8n.cloud/workflow/EWnZg3svZWwcIRs4`
